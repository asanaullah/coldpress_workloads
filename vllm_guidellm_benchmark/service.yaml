apiVersion: v1
kind: Service
metadata:
  name: vllm-server
  namespace: coldpress-project
spec:
  selector:
    app: vllm-server
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      name: http
